# Expo + pnpm monorepo: hoist all dependencies so Expo autolinking and Metro
# resolve native modules consistently across packages. See:
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
approve-builds[]=esbuild
approve-builds[]=unrs-resolver
